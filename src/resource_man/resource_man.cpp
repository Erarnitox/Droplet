#include "resource_man.hpp"

/*
auto resource_man::register_global_slash_commands(std::vector<drop_slash_command>& command_list) -> void {

}

auto resource_man::handle_global_slash_commands(const dpp::slashcommand_t& event) -> void {
	if(event.command.get_command_name() == "drop") {
		event.reply("Just a drop of Water in an endless sea");
	}
}*/
