// Add JavaScript code for any interactivity or dynamic behavior here
// For example, you can add event listeners or make AJAX requests.

// Example: Adding an event listener to a button
document.addEventListener('DOMContentLoaded', function () {
    const addBotButton = document.querySelector('.cta .btn');

    if (addBotButton) {
        addBotButton.addEventListener('click', function (event) {
            // Handle button click (e.g., open OAuth dialog)
        });
    }
});